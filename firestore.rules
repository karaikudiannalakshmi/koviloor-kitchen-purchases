rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Same pattern you use elsewhere: password gate -> signInAnonymously -> auth != null
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
